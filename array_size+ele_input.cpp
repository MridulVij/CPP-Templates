//Code Name
//array size and element by user
//To take input from user - of size of an array and elements of an array will be inserted according to size of array

//Explanation
// in this n is size variable of an array
// and int i variable will increment according to size of an array "n"
//in last it will store array elements one by one in it
//CODE
int n;
    cin>>n;
int a[n];
    for(int i=0; i<n; i++)
    {
        cin>>a[i];
    }
